# Statusline Studio — documentation

The root `README.md` is the guide: how it works, what is simulated, the project layout, running and deploying. This folder holds the assets it embeds: `demo.gif` (six example status lines) and `studio.jpg` (the Studio). New guides go here.
